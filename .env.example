# Optional OpenAI-compatible advisor. When configured, structured shipment state,
# allowed actions, retrieved evidence, and the operator question are sent to this endpoint.
# The deterministic local advisor is used when either endpoint or key is absent.
CCG_LLM_ENDPOINT=
CCG_LLM_API_KEY=
CCG_LLM_MODEL=

# Local server configuration
PORT=4173
